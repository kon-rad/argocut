/**
 * ArgoCut is a fork with a repository and no separate social presence, so the
 * repo is the only link. An X handle or Discord invite that does not exist is
 * worse than no link at all — and the upstream OpenCut ones are not ours.
 */
export const SOCIAL_LINKS = {
	github: "https://github.com/kon-rad/argocut",
};

/** Where to raise a bug, a question, or anything else. */
export const ISSUES_URL = "https://github.com/kon-rad/argocut/issues";
